describe.skip("Select Item", () => {
  beforeEach(() => {
    cy.visit("/");
  });

  it("should render a select button for each item", () => {});

  describe("when clicking the select button", () => {
    it("should disable the button", () => {});
    it("should display the item description", () => {});
    it("should deselect the previously selected item", () => {});
  });
});
